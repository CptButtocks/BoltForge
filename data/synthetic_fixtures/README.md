# Synthetic Fixtures

Use this directory for generated parser and runtime fixtures that contain no
copyrighted game data.

Allowed examples:

- tiny byte streams with made-up headers,
- synthetic WAD-like compression cases,
- fake asset manifests,
- generated meshes or textures with trivial geometry/colors.

Do not place real extracted game bytes here.
