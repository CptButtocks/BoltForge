# Tools

Top-level tool areas mirror the project plan. C++ source may live under `src/`
until each tool becomes substantial enough to split into its own target.

All tools must keep user-provided game data local and opt-in.
