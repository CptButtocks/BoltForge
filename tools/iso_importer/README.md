# ISO Importer

Planned responsibility:

- verify user-owned disc images,
- parse ISO9660 and `SYSTEM.CNF`,
- read raw sectors,
- match known builds from `data/disc_database.yml`,
- produce local-only import reports and manifests.

Do not commit real ISO-derived output.
